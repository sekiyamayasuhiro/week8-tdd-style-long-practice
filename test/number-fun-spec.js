const chai = require("chai");
const expect = chai.expect;

describe("returnsThree(num)", () => {
    it("should return the number 3 if it is 3", () => {
        const num1 = 3;
        const result1 = returnsThree(num1);

        expect(result1).to.equal("3");
    });
});

describe("reciprocal(num)", () => {
    it("should return the reciprocal of num", () => {

    })
})
